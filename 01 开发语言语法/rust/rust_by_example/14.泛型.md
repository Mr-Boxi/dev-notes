### 14.1 泛型

```;
// 一个具体类型 A
struct A;

struct Single(A);

struct SingleGen<T>(T);

fn main() {
	// Single 是具体类型，并显式地使用A
	let _s = Single(A);
	// 这里的 `SingleGen` 的类型参数是显式指定的
	let _char: SingleGen<char> = SingleGen('a');
}
```

